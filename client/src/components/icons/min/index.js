import React from 'react'
import {Place} from 'components/icons'
import * as Types from 'components/mountainInformationNetwork/types'
import * as Colors from './colors'

export const Quick = <Place color={Colors.QUICK} />
export const Avalanche = <Place color={Colors.AVALANCHE} />
export const Snowpack = <Place color={Colors.SNOWPACK} />
export const Weather = <Place color={Colors.WEATHER} />
export const Incident = <Place color={Colors.INCIDENT} />
