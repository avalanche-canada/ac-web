
div.container(ng-controller='NavbarCtrl')
    div#menu(multi-level-push-menu='')

    div#page-wrap(navbar-header-menu='')

    ///multi-level-push-menu#menu
    ///navbar-header-menu#page-wrap






    // div.navbar-header
    //  button.navbar-toggle(type='button', ng-click='isCollapsed = !isCollapsed')
    //    span.sr-only Toggle navigation
    //    span.icon-bar
    //    span.icon-bar
    //    span.icon-bar
    //  a.navbar-brand(href='/') avalanche-canada

    // div#navbar-main.navbar-collapse.collapse(collapse='isCollapsed')
    //  ul.nav.navbar-nav
    //    li(ng-repeat='item in menu', ng-class='{active: isActive(item.link)}')
    //      a(ng-href='{{item.link}}') {{item.title}}
